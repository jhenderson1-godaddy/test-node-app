# Minimal Node App — environment variables
#
# Copy to `.env` for LOCAL development only. Do NOT commit `.env` and do NOT
# include it in a deployment zip — set these in the Node.js Hosting UI instead.

# PORT is injected by the hosting platform. Locally it defaults to 3000.
PORT=3000

# Optional display name shown in the UI.
APP_NAME=Minimal Node App
